# Set these only when the backend is hosted separately (e.g. Vercel frontend).
# Local Vite and Docker use same-origin proxies; leave blank locally.
VITE_API_URL=
VITE_WS_URL=

# Public browser configuration only. Never use a service-role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
